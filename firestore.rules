rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Portfolio media documents: readable by all visitors, writable by client
    match /portfolio_media/{mediaId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
